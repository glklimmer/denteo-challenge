# Denteo-Challenge

## Getting Started

First we need to setup the database. Use this

`bin/rails db:setup`.

To start the server use

`bin/rails server`.

Then navigate in your browser to

`localhost:3000`.
